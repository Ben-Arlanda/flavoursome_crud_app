<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/restaurant_info.css">
</head>

<body>
    <title>
        <%= restaurant.name %>
    </title>
    <header>

        <div class="navbar">
            <a href="/">Home</a>
            <a href="/restaurants">Restaurants</a>
            <a href="#reviews">Reviews</a>
            <a href="#contact">Contact</a>
        </div>
        <h1>
            <%= restaurant.name %>
        </h1>
    </header>

    <div class="restaurant-info">
        <img src="<%= restaurant.image_url1 %>" alt="Restaurant Image 1">
        <img src="<%= restaurant.image_url2 %>" alt="Restaurant Image 2">

        <p>
            <%= restaurant.description %>
        </p>
        <p><strong>Location:</strong>
            <%= restaurant.location %>
        </p>
        <p><strong>Average Rating:</strong>
            <%= averageRating.toFixed(2) %>
        </p>
    </div>

    <div class="reviews-container">
        <h2>Reviews</h2>
        <% for (const review of reviews) { %>
            <div class="review">

                <!-- <%= review.email %> -->
                </p>

                <%= review.description %>
                    </p>
                    <p><strong>Rating:</strong>
                        <%= review.rating %>
                    </p>

                    <form action="/reviews/<%= review.id %>?_method=delete" method="post">
                        <input type="hidden" name="restaurantId" value="<%= restaurant.id %>">
                        <button type="submit">Delete Review</button>
                    </form>
            </div>
            <% } %>
    </div>

    <div class="new-review">
        <h2>Create a New Review</h2>
        <form action="/reviews/<%= restaurant.id %>/reviews" method="post">
            <input type="hidden" name="" value="<%= restaurant.id %>">
            <input type="hidden" name="email">
            <label for="description">Description:</label>
            <textarea id="description" name="description" rows="4" required></textarea>

            <label for="rating">Rating:</label>
            <input type="number" id="rating" name="rating" min="1" max="5" required>

            <button type="submit">Post</button>
        </form>
    </div>

</body>

</html>








